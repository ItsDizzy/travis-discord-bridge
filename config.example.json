{
  "server": {
    "host": "0.0.0.0",
    "port": 3000
  },
  "repositories": [
    {
      "name": "yts-api",
      "branches": ["master"],
      "hooks": ["pending", "passed", "fixed", "broken", "failed", "still_failing", "canceled"],
      "webhook": ""
    }
  ]
}